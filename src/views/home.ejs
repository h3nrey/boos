<!DOCTYPE html>
<html lang="en">
<%-include ("./partials/head.ejs") %> 
<body>
    <% if (user != null) { %>
        <h1>Home <%-user.username%></h1>     
    <% } else {%>
        <p>Esse usuário não existe.</p>
        <a href="/"> voltar</a>
    <% } %>
</body>
</html>